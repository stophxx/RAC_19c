# 8.DB 소프트웨어 설치(2026.04)


## 설치파일 압축 해제

```
[oracle@jjh1 ~]$ cd $ORACLE_HOME
[oracle@jjh1 19c]$ unzip ~/LINUX.X64_193000_db_home.zip -d $ORACLE_HOME
```

## Response File(rsp) 생성 및 수정

Oracle Database Software Silent 설치를 위해  
response file(`db_install.rsp`)을 사용하였다.

처음부터 response file을 직접 작성하지 않고,  
GUI 기반 설치를 먼저 수행한 뒤 생성된 response file을 추출하였다.


이후 실습 환경에 맞게 주요 항목들을 수정하여  
Silent 설치에 재사용하였다.

수정한 주요 항목은 다음과 같다.

- Oracle Base / Oracle Home 경로
- RAC Database Software 설치 옵션
- Cluster Node 정보
- OSDBA / OSOPER 그룹 설정
- Oracle Inventory 경로
- Edition 정보
- Root Script 자동 실행 옵션

주요 설정 내용은 다음과 같다.

```properties
oracle.install.option=INSTALL_DB_SWONLY
ORACLE_BASE=/u01/app/oracle
ORACLE_HOME=/u01/app/oracle/product/19c

oracle.install.db.InstallEdition=EE

oracle.install.db.OSDBA_GROUP=dba
oracle.install.db.OSOPER_GROUP=dba
oracle.install.db.OSBACKUPDBA_GROUP=dba
oracle.install.db.OSDGDBA_GROUP=dba
oracle.install.db.OSKMDBA_GROUP=dba
oracle.install.db.OSRACDBA_GROUP=dba

oracle.install.db.CLUSTER_NODES=jjh1,jjh2

oracle.install.db.rootconfig.executeRootScript=true
oracle.install.db.rootconfig.configMethod=SUDO
oracle.install.db.rootconfig.sudoPath=/usr/bin/sudo
oracle.install.db.rootconfig.sudoUserName=root
```

실습 환경에서는 hosts 기반 SCAN 구성을 사용하였으며,

DNS/NIS 관련 warning은 Ignore 옵션으로 진행하였다.

또한 Database Software 설치 단계에서는 특정 인스턴스(SID)가 아직 생성되지 않은 상태이므로,
기존 환경 변수에 설정되어 있던 `ORACLE_SID`를 해제한 뒤 설치를 진행하였다.

```
[oracle@jjh1 19c]$ unset ORACLE_SID
```


## Oracle Database Software Silent 설치

```
[oracle@jjh1 19c]$ ./runInstaller -silent -responseFile ~/db_install.rsp -ignorePrereqFailure
```

설치 진행 중 root script 자동 실행 단계에서
root 비밀번호 입력을 요구한다.

root 비밀번호를 입력하면
각 노드의 root script가 자동 수행된다.

<img width="908" height="256" alt="image" src="https://github.com/user-attachments/assets/c2d9a36b-cef6-4cf3-a4f9-4113db897292" />

설치 로그는 `/u01/app/oraInventory/logs` 경로에서 확인하였다.

설치 완료 후 다음 명령으로 정상 설치 여부를 확인하였다.

```bash

[oracle@jjh1 ~]$ $ORACLE_HOME/OPatch/opatch lsinventory

[oracle@jjh1 ~]$ sqlplus -v

```

<img width="387" height="124" alt="image" src="https://github.com/user-attachments/assets/8da7c759-9877-4fae-96e6-cb8fb4f09982" />
<img width="353" height="152" alt="image" src="https://github.com/user-attachments/assets/c5dfff08-520d-4133-a3eb-40313c8ea0fa" />


Oracle Database Software 설치가 정상 완료된 것을 확인하였다.
