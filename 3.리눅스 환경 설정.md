# **3.리눅스 환경 설정(2026.04.20)**

- **모바텀(mobaxterm)에 접속해본다.**

세션 - SSH에서 private ip (192.168.56.101)로 접속한다.

---
- **리눅스 설치 정보를 확인한다.**

```
[root@jjh1 ~]# uname -a
[root@jjh1 ~]# cat /etc/os-release
```

<img width="460" height="272" alt="image" src="https://github.com/user-attachments/assets/06e13b24-811d-4d93-9685-74b4dbf7413d" />

---
- **불필요한 서비스 정리**

```
### 방화벽 중지
systemctl stop firewalld
systemctl disable firewalld

### 블루투스 중지
systemctl stop bluetooth
systemctl disable bluetooth

### 시간 동기화 중지
systemctl stop chronyd
systemctl disable chronyd
mv /etc/chrony.conf /etc/chrony.conf.bak

### NTP 중지
systemctl stop ntpdate
systemctl disable ntpdate

### AVAHI 중지
systemctl stop avahi-daemon.socket
systemctl disable avahi-daemon.socket
systemctl stop avahi-daemon
systemctl disable avahi-daemon

### 가상 시스템 관리 중지
systemctl stop libvirtd
systemctl disable libvirtd
```

---
- **SELINUX permissive 설정**

```
[root@jjh1 ~]# vi /etc/selinux/config
```

SELINUX=permissive로 변경한다.

---
- **hosts 파일 수정**
```
[root@jjh1 ~]# vi  /etc/hosts
127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6



### Public
10.0.2.25  jjh1
10.0.2.26  jjh2
### Private
192.168.56.103  jjh1-priv
192.168.56.104  jjh2-priv
### Virtual
10.0.2.125  jjh1-vip
10.0.2.126  jjh2-vip
### SCAN
10.0.2.130  jjh-scan
10.0.2.131  jjh-scan
10.0.2.132  jjh-scan

```

---
- **dnsmasq 활성화하기(안해도 되는 듯 함)**

```
[root@jjh1 ~]# vi /etc/dnsmasq.conf
```

local=/localdomain/ 로 변경한다.( vi 창에서 /local 로 검색 가능. n키로 다음으로 넘어가기 가능)

<img width="563" height="59" alt="image" src="https://github.com/user-attachments/assets/cfb013f8-f2df-43b2-aa42-7dfad15467eb" />

```
[root@jjh1 ~]# systemctl start dnsmasq
[root@jjh1 ~]# systemctl enable dnsmasq

[root@jjh1 ~]# nslookup jjh-scan
```

nslookup 명령어 수행을 위해 재부팅이 필요하면 수행한다.

**resolv.conf 내용을 확인한다.**

```
[root@jjh1 ~]# cat /etc/resolv.conf
```

---
- **temp 파일 시스템을 할당한다.**

```
[root@jjh1 ~]# vi /etc/fstab

#아래의 한 줄을 추가한다.
tmpfs                   /dev/shm                tmpfs   size=7g         0 0
```

<img width="683" height="81" alt="image" src="https://github.com/user-attachments/assets/02738535-ba9c-4e4a-96bb-d039a2c57d53" />

```
[root@jjh1 ~]# mount -o remount /dev/shm
[root@jjh1 ~]# df -h /dev/shm
```

remount하여 적용시킨다.

---
- **preinstall 패키지 설치**

오라클 설치 전에 preinstall 패키지를 설치한다.

```
[root@jjh1 ~]# yum install -y oracle-database-preinstall-19c
```

<img width="912" height="234" alt="image" src="https://github.com/user-attachments/assets/7f36bb63-45b0-4354-b57c-bca4d47da050" />

<img width="887" height="97" alt="image" src="https://github.com/user-attachments/assets/010121f8-4aac-471c-89f4-f27395f23d1a" />

---
**※yum이 안된다면 외부와 인터넷이 연결이 되지 않은 것이다!**

**ping google.com 또는 ping yum.oracle.com을 했을 때 핑이 나가야 한다.**

**인터넷이 안된다면, 어댑터1이 NAT 네트워크(또는 NAT)로 설정되어있는지,**

**어댑터1의 DNS주소가 호스트(윈도우)의 cmd창에서 ipconfig -all 명령어로 나온 DNS주소와 같은지 확인한다.**

** 그래도 ping이 안 될 때 **

VirtualBox에서 설정 → 네트워크 → 어댑터1
→ 다음에 연결됨: NAT 네트워크  →  NAT 으로 변경 

게이트웨이 수정 (딱 2줄)
```
nmcli con mod enp0s3 ipv4.gateway 10.0.2.2
nmcli con up enp0s3
```

---

- **그룹 및 패스워드 설정**

```
[root@jjh1 ~]# usermod -g dba -G dba oracle
[root@jjh1 ~]# passwd oracle
```

oracle 유저를 dba 그룹의 멤버로 추가한다.

---
- **오라클 소프트웨어 설치를 위한 디렉토리 생성**

```
mkdir -p /home/oracle/media
mkdir -p /u01/app/oracle/product/19c
mkdir -p /u01/app/grid/19c
mkdir -p /u01/oraInventory
mkdir -p /oraarch
chown -R oracle:dba /u01
chmod -R 775 /u01
chown -R oracle:dba /oraarch
chmod -R 775 /oraarch
chown -R oracle:dba /dev
```

---
- **ASMLib 설치**

https://www.oracle.com/linux/downloads/linux-asmlib-v8-downloads.html 에서 
oracleasmlib-2.0.17-1.el8.x86_64.rpm를 다운로드하고 모바텀을 이용해 리눅스에 올린다.

https://yum.oracle.com/repo/OracleLinux/OL8/addons/x86_64/index.html 에서 
oracleasm-support-2.1.12-1.el8.x86_64.rpm을 다운로드하고 모바텀을 이용해 리눅스에 올린다.

두 rpm 파일을 설치한다.

```
[root@jjh1 ~]# rpm -ivh oracleasmlib-2.0.17-1.el8.x86_64.rpm
[root@jjh1 ~]# rpm -ivh oracleasm-support-2.1.12-1.el8.x86_64.rpm
```

---
- **공유메모리 값 설정**

/etc/sysctl.conf의 shmmax, shmall 값을 메모리값의 절반인 4GB= 4294967296(바이트) 로 수정하고 값을 확인한다.

```
[root@jjh1 ~]# vi /etc/sysctl.conf
```

kernel.shmall=4294967296
kernel.shmmax=4294967296

```
[root@jjh1 ~]# /sbin/sysctl -p
```

shmmax = 단일 프로세스가 공유 메모리를 호출하기 위한 최대 값

shmall = 모든 프로세스가 사용할 수 있는 총 공유 메모리 값

---
- **root 유저의 .bash_profile 수정하기**

```bash
export ORACLE_BASE=/u01/app/oracle;
export GRID_HOME=/u01/app/grid/19c;
export ORACLE_HOME=$ORACLE_BASE/product/19c;
export PATH=$PATH:$GRID_HOME/bin;
```

---
- **oracle 유저의 .bash_profile 수정**

root유저에서 oracle유저의 .bash_profile을 수정한다. 아니면 oracle 유저로 접속해서 수정해도 된다.

```bash
[root@jjh1 ~]# vi ~oracle/.bash_profile

export ORACLE_BASE=/u01/app/oracle;
export ORACLE_HOME=$ORACLE_BASE/product/19c;
export ORACLE_SID=jjh1;        #2번노드 jjh2
export ORACLE_HOSTNAME=jjh1;   #2번노드 jjh2
export ORACLE_UNQNAME=jjhdb;
export GRID_HOME=/u01/app/grid/19c;
export GRID_SID=+ASM1;         #2번노드 ASM2
export PATH=$ORACLE_HOME/bin:$GRID_HOME/bin:$PATH
export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib;
export CLASSPATH=$ORACLE_HOME/jlib:$ORACLE_HOME/rdbms/jlib;
export EDITOR=vi;

alias grid='export ORACLE_HOME=$GRID_HOME; export ORACLE_SID=$GRID_SID; export PATH=$ORACLE_HOME/bin:$GRID_HOME/bin:$PATH; echo $ORACLE_SID; echo $ORACLE_HOME'
alias db='. ~oracle/.bash_profile;export PATH=$ORACLE_HOME/bin:$GRID_HOME/bin:$PATH; echo $ORACLE_SID;echo $ORACLE_HOME'
alias oh='cd $ORACLE_HOME;pwd'
alias sys='sqlplus / as sysdba'
```
