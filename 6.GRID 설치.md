# 6.GRID 설치(2026.04.22)

## 설치파일 압축해제

아래의 링크에서 Oracle Grid, Oracle Database를 다운로드하고 리눅스에 업로드한다.

https://www.oracle.com/kr/database/technologies/oracle19c-linux-downloads.html

<img width="1022" height="431" alt="image" src="https://github.com/user-attachments/assets/7cfd67dc-0d96-468c-b00e-b4eb06c24617" />

1번 노드에 oracle 유저로 접속해서 업로드한 grid ZIP 파일을 해제한다.

```
[root@jjh1 ~]# su - oracle
[oracle@jjh1 ~]$ unzip /home/oracle/media/LINUX.X64_193000_grid_home.zip -d $GRID_HOME
```


## cvuqdisk 패키지 설치

root 유저로 접속해서 다음의 패키지를 설치한다.

2번노드에도 전송해서 설치한다.

```
#root 유저로 접속
[root@jjh1 ~]# cd $GRID_HOME/cv/rpm
[root@jjh1 rpm]# rpm -ivh cvuqdisk-1.0.10-1.rpm


#2번 노드로 rpm 전송
[root@jjh1 rpm]# scp cvuqdisk-*.rpm jjh2:/tmp
[root@jjh1 rpm]# ssh root@jjh2

#cvuqdisk 패키지 설치 (2번 노드)
[root@jjh2 ~]# cd /tmp
[root@jjh2 tmp]# rpm -ivh cvuqdisk-*.rpm
[root@jjh2 tmp]# exit
```

## oracle 유저가 비밀번호 없이 SSH 접속가능하게 설정

```
[oracle@jjh1 ~]$ cd $GRID_HOME/oui/prov/resources/scripts
[oracle@jjh1 scripts]$ ./sshUserSetup.sh -user oracle -hosts "jjh1 jjh2" -noPromptPassphrase -advanced
```
값을 입력하는 창이 나오면 yes를 치고, 

1번노드 oracle패스워드, 2번노드 oracle패스워드를 입력한다. (총 4번)

<img width="574" height="228" alt="image" src="https://github.com/user-attachments/assets/cbe2354d-2bb3-454b-9c47-e0575d7de6c0" />
<img width="562" height="242" alt="image" src="https://github.com/user-attachments/assets/f735bbb5-e2f9-4cd0-a3a0-e9393b45993f" />

## Response File(rsp) 생성 및 수정

Oracle Grid Infrastructure Silent 설치를 위해 response file(`grid.rsp`)을 사용하였다.

처음부터 직접 response file을 작성하지 않고,  
기존 GUI 기반 설치 레퍼런스를 참고하여 설치를 먼저 수행한 뒤,
설치 마지막 단계에서 생성된 response file을 추출하였다.

추출된 rsp 파일을 기반으로 현재 실습 환경에 맞게 값을 수정하여
Silent 설치에 재사용하였다.

수정한 주요 항목은 다음과 같다.

- Cluster Name
- SCAN Name
- Cluster Node 정보
- Network Interface 설정
- ASM Disk Group 정보
- Oracle Base / Inventory 경로
- Root Script 자동 실행 옵션
- GNS 비활성화


```
oracle.install.responseFileVersion=/oracle/install/rspfmt_crsinstall_response_schema_v19.0.0

INVENTORY_LOCATION=/u01/app/oraInventory
oracle.install.option=CRS_CONFIG
ORACLE_BASE=/u01/app/oracle

oracle.install.asm.OSDBA=asmdba
oracle.install.asm.OSOPER=dba
oracle.install.asm.OSASM=asmadmin

oracle.install.crs.config.scanType=LOCAL_SCAN
oracle.install.crs.config.gpnp.scanName=jjh-scan
oracle.install.crs.config.gpnp.scanPort=1521

oracle.install.crs.config.ClusterConfiguration=STANDALONE
oracle.install.crs.config.clusterName=jjhdb
oracle.install.crs.config.gpnp.configureGNS=false
oracle.install.crs.config.autoConfigureClusterNodeVIP=false

oracle.install.crs.config.clusterNodes=jjh1:jjh1-vip,jjh2:jjh2-vip
oracle.install.crs.config.networkInterfaceList=enp0s3:10.0.2.0:1,enp0s8:192.168.56.0:5

oracle.install.crs.configureGIMR=false
oracle.install.asm.configureGIMRDataDG=false
oracle.install.crs.config.storageOption=FLEX_ASM_STORAGE

oracle.install.crs.config.useIPMI=false

oracle.install.asm.SYSASMPassword=oracle
oracle.install.asm.monitorPassword=oracle

oracle.install.asm.diskGroup.name=CRS
oracle.install.asm.diskGroup.redundancy=NORMAL
oracle.install.asm.diskGroup.AUSize=4
oracle.install.asm.diskGroup.disks=/dev/oracleasm/disks/CRS1,/dev/oracleasm/disks/CRS2,/dev/oracleasm/disks/CRS3
oracle.install.asm.diskGroup.diskDiscoveryString=/dev/oracleasm/disks

oracle.install.asm.configureAFD=false
oracle.install.crs.configureRHPS=false
oracle.install.config.managementOption=NONE

oracle.install.crs.rootconfig.executeRootScript=true
oracle.install.crs.rootconfig.configMethod=SUDO
oracle.install.crs.rootconfig.sudoPath=/usr/bin/sudo
oracle.install.crs.rootconfig.sudoUserName=root
```

## Cluster Verification Utility(CVU) 사전 검증

Grid Infrastructure 설치 전 CVU(Cluster Verification Utility)를 이용하여
노드 간 호환성 및 prerequisite 항목을 사전 점검하였다.

```bash
[oracle@jjh1 ~]$ cd $GRID_HOME

[oracle@jjh1 19c]$ ./runcluvfy.sh comp peer \
-n jjh1,jjh2 \
-verbose
```

검증 항목:
- Physical Memory
- Swap Space
- User / Group Consistency
- Network Interface
- SSH User Equivalence
- Shared Storage
- Run Level

또한 Grid 설치 전 prerequisite check를 수행하였다.

```bash
[oracle@jjh1 19c]$ ./runcluvfy.sh stage -pre crsinst \
-n jjh1,jjh2 \
-verbose
```

검증 과정에서 다음과 같은 이슈를 확인 및 수정하였다.

- Passwordless SSH 오류
- SCAN Name Resolution 오류
- SCP Protocol 충돌
- DNS/NIS Warning
- resolv.conf consistency 오류

  
## Oracle Linux 8 환경 SCP Protocol 충돌 해결

Oracle Linux 8 환경에서는 OpenSSH 버전 차이로 인해
Silent 설치 중 다음과 같은 SSH/SCP 관련 오류가 발생할 수 있다.

```text
INS-44000
INS-06006
protocol error: filename does not match request
```

이를 해결하기 위해 `/usr/bin/scp` wrapper를 생성하였다.

```bash
[root@jjh1 ~]# mv /usr/bin/scp /usr/bin/scp.orig

[root@jjh1 ~]# vi /usr/bin/scp
```

```bash
#!/bin/bash
/usr/bin/scp.orig -T "$@"
```

```bash
[root@jjh1 ~]# chmod 555 /usr/bin/scp
```

모든 설치 완료 후 원복한다.

```bash
[root@jjh1 ~]# mv /usr/bin/scp.orig /usr/bin/scp
[root@jjh1 ~]# chmod 755 /usr/bin/scp
```

---

## Oracle Linux 8 OS Check 우회

Oracle 19c는 Oracle Linux 8.9 환경에서
OS Version Check 오류가 발생할 수 있으므로
다음 환경 변수를 사용하였다.

```bash
[oracle@jjh1 ~]$ vi ~/.bash_profile
```

```bash
export LANG=en_US.UTF-8
export CV_ASSUME_DISTID=OEL8.4
```

```bash
[oracle@jjh1 ~]$ source ~/.bash_profile
```

---

  
## Grid Infrastructure Silent 설치

```
[oracle@jjh1 ~]$ cd $GRID_HOME
[oracle@jjh1 19c]$ ./gridSetup.sh -silent -ignorePrereqFailure -responseFile ~/grid.rsp
```
<img width="888" height="301" alt="image" src="https://github.com/user-attachments/assets/80052000-8b49-4190-9c38-e7e85a3891ff" />
Enter password for user root :

까지 온 거면 root script 자동 실행 단계로 넘어간 상태

root 비밀번호 입력

---

GRID 설치 완료 후 

설치가 제대로 완료되었는지 확인한다.

```
[root@jjh1 ~]# crsctl stat res -t
```

<img width="669" height="872" alt="image" src="https://github.com/user-attachments/assets/a0fd44bc-8bfd-4b36-a03f-85a65a60280b" />

```
# cluster 상태 확인
[root@jjh1 ~]# crsctl check cluster -all

# 노드 확인
[root@jjh1 ~]# olsnodes -n

```

<img width="510" height="231" alt="image" src="https://github.com/user-attachments/assets/e10c8473-cd69-4ca9-8d9e-94ee3a695a0e" />

```
# ASM 확인 oracle(grid)에서

[oracle@jjh1 ~]$ alias grid
alias grid='export ORACLE_HOME=$GRID_HOME; export ORACLE_SID=$GRID_SID; export PATH=$ORACLE_HOME/bin:$GRID_HOME/bin:$PATH; echo $ORACLE_SID; echo $ORACLE_HOME'

[oracle@jjh1 ~]$ grid
+ASM1
/u01/app/grid/19c

[oracle@jjh1 ~]$ sqlplus / as sysasm

SQL> select name,state,type,total_mb,free_mb from v$asm_diskgroup;
```
<img width="591" height="348" alt="image" src="https://github.com/user-attachments/assets/38f3e76f-8882-402c-8905-14b3a127d282" />



CRS 디스크를 확인한다.

```
[oracle@jjh1 ~]$ ocrcheck
```
![image](https://github.com/oraclejyp/19c_rac_inst/assets/133745372/41ca6ad0-03b6-40e0-8f37-f8535f7b4718)
<img width="639" height="332" alt="image" src="https://github.com/user-attachments/assets/1985c5cc-2857-4a06-9309-47c01ba44814" />

---

## 설치 실패 후 ASM 디스크 초기화 참고

Grid Infrastructure 설치 중 오류가 발생한 경우,  
CRS용 ASM 디스크가 기존 Disk Group의 `MEMBER` 상태로 남아 있을 수 있다.

이 경우 재설치 시 해당 디스크가 `CANDIDATE`가 아닌 `MEMBER`로 표시되어  
Grid 설치 화면에서 새 CRS Disk Group용 디스크로 선택할 수 없다.

디스크 상태는 다음 명령으로 확인할 수 있다.

```sql
SELECT path, header_status, mode_status, state
FROM v$asm_disk;
```

| HEADER_STATUS | 의미 |
|---|---|
| CANDIDATE | ASM Disk Group에 아직 사용되지 않은 디스크 |
| MEMBER | 이미 ASM Disk Group에 포함된 디스크 |
| FORMER | 이전에 ASM Disk Group으로 사용된 이력이 있는 디스크 |

만약 Grid 설치 도중 에러가 발생해서 다시 설치를 하려고 하는데,

CRS용 ASM 디스크가 기존 Disk Group의 MEMBER 상태로 남아 있어

새 설치 시 Candidate Disk로 선택되지 않는 경우에는

(candidate(후보)가 아니라 member로 이미 할당되어 있는 경우)


VirtualBox에서 각 서버에 연결되어있는 공유 디스크들의 연결을 삭제하고, 

파일 - 도구 - 가상 미디어 관리자로 들어가서 해당 공유 디스크를 지운다.

그리고 나서 앞서 했던 단계 중 공유 스토리지 생성부분만 다시 수행한다.

재수행 범위는 다음과 같다.

1. CMD에서 공유 디스크 생성
2. 공유 가능(shared) 설정
3. Linux에서 `fdisk`로 파티션 생성
4. `oracleasm createdisk`로 ASM 디스크 등록
5. `oracleasm scandisks`, `oracleasm listdisks` 확인

![image](https://github.com/oraclejyp/19c_rac_inst/assets/133745372/6037eb65-115e-421a-b53e-4fd5169cab66)

![image](https://github.com/oraclejyp/19c_rac_inst/assets/133745372/7c499491-ab6a-4d99-a2fb-8e6357a68087)



